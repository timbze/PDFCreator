<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="RoundDarkBackgroundTextboxControlTemplate" TargetType="ContentControl">
        <Border BorderThickness="1"
                Background="{TemplateBinding Background}"
                CornerRadius="5">
            <TextBlock
                Text="{TemplateBinding Content}"
                Foreground="{TemplateBinding Foreground}"
                Padding="5 2" />
        </Border>
    </ControlTemplate>

    <Style x:Key="RoundDarkBackgroundTextboxStyle" TargetType="ContentControl">
        <Setter Property="Template" Value="{StaticResource RoundDarkBackgroundTextboxControlTemplate}" />
    </Style>
</ResourceDictionary>
