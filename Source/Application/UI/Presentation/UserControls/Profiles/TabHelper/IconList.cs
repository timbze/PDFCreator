namespace pdfforge.PDFCreator.UI.Presentation.UserControls.Profiles.TabHelper
{
    public enum IconList
    {
        // Enum value must be the same as the x:Key of the desired Icon!
        DebugSettings,

        GeneralSettings,
        TitleReplacementSettings,
        LicenseSettings,
        RssFeedArrowIcon,
        RssFeedIcon,
        DefaultViewerSettings
    }
}
