# This is the password file template.
# This is used to provide passwords and other values required in integration tests
# You can copy this to password.txt and insert the required passwords there
# Passwords are entered in the form (without the leading #)
# name = value

ftp_server = 
ftp_user =
ftp_password = 

smtp_server =
smtp_user =
smtp_email =
smtp_password =
smtp_port =

imap_server =
imap_user =
imap_password =
imap_port =